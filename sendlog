#!/bin/bash

. $HOME/backup.cfg

if [ -f $commonlog ]; then

	if [ x"$send_common_log" = "xy" ]; then
		send_report 'Nightly log for '$wuser' at '$whost $commonlog;
	fi

	rm -f $commonlog
	rm -f $mlog
fi
